#!/bin/sh

set -eux

gup --always
gup -u all ../tools/hub

tag=$(git describe --tags)
assets=""
for i in *; do
    assets="$assets -a $i"
done

hub release create $assets -c master -m "${tag}" "${tag}"
